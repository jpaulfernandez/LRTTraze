<Page x:ConnectionId='1'
    x:Class="IntelligentKioskSample.Views.VisionApiExplorer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IntelligentKioskSample.Views"
    xmlns:ctl="using:IntelligentKioskSample.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                   
    mc:Ignorable="d">

    <Page.Resources>
        <ctl:BooleanToVisibilityConverter x:Key="bolleanToVisibilityConverter"/>
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          EntranceNavigationTransitionInfo.IsTargetElement="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar x:ConnectionId='2' x:Name="commandBar" VerticalAlignment="Top" HorizontalContentAlignment="Stretch">
           
            <AppBarButton Icon="Favorite" Label="Suggestions">
                <AppBarButton.Flyout>
                    <Flyout x:ConnectionId='22' x:Name="favoriteImagePickerFlyout">
                        <GridView x:ConnectionId='23' x:Name="favoritePhotosGridView" SelectionMode="Single"                                               Width="380">
                            <GridView.ItemTemplate>
                                <DataTemplate>
                                    <Image Width="120" Height="120" Source="{Binding}"/>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>

            <AppBarButton x:ConnectionId='18' Icon="Video" Label="WebCam"                               />
            
            <AppBarButton x:ConnectionId='19' Icon="Globe" Label="Search" x:Name="PicturesAppBarButton" >
                <AppBarButton.Flyout>
                    <Flyout x:ConnectionId='20' x:Name="imageSearchFlyout">
                        <ctl:ImageSearchUserControl x:ConnectionId='21' ImageContentType="" ClearStateWhenClosed="False"                                                                                              />
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>

            <CommandBar.Content>
                <ctl:PageHeader>
                    <ctl:PageHeader.HeaderContent>
						<TextBlock Text="Vision API Explorer" Style="{ThemeResource PageTitleTextBlockStyle}" TextWrapping="NoWrap"/>
                    </ctl:PageHeader.HeaderContent>
                </ctl:PageHeader>
            </CommandBar.Content>
        </CommandBar>

        <TextBlock x:ConnectionId='3' x:Name="landingMessage" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Select a photo to start" Style="{StaticResource HeaderTextBlockStyle}"/>

        <Grid Grid.Row="1"  >
            <Grid.RowDefinitions>
                <RowDefinition Height="0.1*"/>
                <RowDefinition/>
                <RowDefinition Height="0.1*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="0.5*"/>
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Row="1" Grid.Column="1" VerticalAlignment="Top">

                <Grid x:ConnectionId='14' x:Name="webCamHostGrid" Visibility="Collapsed" >
                    <ctl:ImageWithFaceBorderUserControl x:ConnectionId='16' BalloonBackground="#ECCF11" BalloonForeground="#D86C2A" FlowDirection="RightToLeft" x:Name="imageFromCameraWithFaces" PerformComputerVisionAnalysis="True" PerformOCRAnalysis="{Binding ElementName=ocrToggle, Path=IsOn}" DetectFacesOnLoad="True"/>
                    <ctl:CameraControl x:ConnectionId='17' x:Name="cameraControl"/>
                </Grid>

                <ctl:ImageWithFaceBorderUserControl x:ConnectionId='15' BalloonBackground="#ECCF11" BalloonForeground="#D86C2A" x:Name="imageWithFacesControl" PerformComputerVisionAnalysis="True" PerformOCRAnalysis="{Binding ElementName=ocrToggle, Path=IsOn}" DetectFacesOnLoad="True"/>

            </Grid>

            <ScrollViewer x:ConnectionId='4' x:Name="resultsDetails" Visibility="Collapsed"  Grid.Row="1" Grid.Column="2" Margin="12,0,12,0" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Vertical">
                    <GridView x:ConnectionId='5' Header="Tags:" x:Name="tagsGridView" IsItemClickEnabled="False" SelectionMode="None">
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Width="120">
                                    <TextBlock TextWrapping="NoWrap" Text="{Binding Name}" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" Foreground="LightGray"/>
                                    <TextBlock Margin="4,0,0,0" Text="{Binding Confidence}" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Gray" />
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="MinHeight" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                    </GridView>

                    <GridView x:ConnectionId='6' Grid.Column="1" Header="Description:" x:Name="descriptionGridView" IsItemClickEnabled="False" SelectionMode="None">
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock TextWrapping="WrapWholeWords" Text="{Binding Description}" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" Foreground="LightGray" />
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{Binding Confidence}" Style="{StaticResource CaptionTextBlockStyle}" Margin="4,0,0,0" Foreground="Gray"/>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="MinHeight" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                    </GridView>

                    <TextBlock Text="Celebrities:"/>
                    <TextBlock x:ConnectionId='7' x:Name="celebritiesTextBlock" TextWrapping="Wrap" Foreground="LightGray" Style="{StaticResource CaptionTextBlockStyle}" />

                    <ListView x:ConnectionId='8' Grid.Column="1" Margin="0,12,0,12" Header="Colors:" x:Name="colorInfoListView" IsItemClickEnabled="False" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Width="160" Text="{Binding Description}" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" Foreground="LightGray" />
                                    <ListView Margin="4,0,0,0" IsItemClickEnabled="False" SelectionMode="None" ItemsSource="{Binding Colors}" >
                                        <ListView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ListView.ItemsPanel>
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Rectangle Width="15" Height="15" Margin="2" Stroke="White" StrokeThickness="1" VerticalAlignment="Center" Fill="{Binding}" />
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="MinHeight" Value="0"/>
                                                <Setter Property="MinWidth" Value="0"/>
                                                <Setter Property="Padding" Value="0"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                            </Style>
                                        </ListView.ItemContainerStyle>
                                    </ListView>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        
                    </ListView>
                    
                    <ToggleSwitch x:ConnectionId='9' Header="OCR:" x:Name="ocrToggle"                       />
                    
                </StackPanel>
            </ScrollViewer>

        </Grid>
    </Grid>
</Page>

